services:
  - type: web
    name: stitchcraft-backend
    runtime: node
    rootDir: stitchcraft-backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
